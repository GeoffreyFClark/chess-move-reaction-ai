# Chess Move Reaction AI - Backend Configuration
# Copy this file to .env and modify as needed

# Server settings
HOST=0.0.0.0
PORT=8000

# CORS (comma-separated origins for production, "*" for development)
CORS_ORIGINS=*

# Stockfish engine settings
# Path to Stockfish executable (optional - auto-detected if in backend dir or PATH)
# STOCKFISH_PATH=/usr/local/bin/stockfish

# Engine analysis depth (higher = more accurate but slower)
STOCKFISH_DEPTH=18

# Maximum time for engine analysis in milliseconds
STOCKFISH_MOVETIME_MS=500

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
